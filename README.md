## API Postman Testing Example ##

These tests are setup to run as a collection in Postman, with a dev environment, API key is saved locally and also as a secret in GitHub, to allow for CI testing via GitHub Actions.
